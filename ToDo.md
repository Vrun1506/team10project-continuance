There is a lot of redundancy in the CI pipeline with all of the environments and retentions and stuff so gonna have to refactor that. 

Pipeline works at the very least though. 


Leaderboard has stopped rendering for some reason. 


We also need to figure out why the game keeps crashing when we set the timer achievement to 25 seconds and a time below 25 seconds crashes the game.